# 统计模块本地检查报告（备注：后续修改）

生成时间：2026-03-04  
范围：数据统计模块（工资统计、加工概况、时长统计、报警统计）

## 本轮已完成

1. 工资统计
- 已补充工资列表行级 `employeeId`、`deviceId` 返回。
- 已实现“日期下钻工资明细”弹窗能力（前端接入 `getSalaryDetail`）。
- 已补充明细字段：设备、花型、花型针数、开始/结束时间、缝纫次数、单价、金额。

2. 状态统计
- 加工概况列表字段已按需求补齐：设备、员工工号/姓名、日期、花型信息、速度、开始时间、加工次数、平均加工时长、花型缝纫次数、报警信息/报警时间、累计开机时长。
- 时长统计列表字段已按需求补齐：设备、员工工号/姓名、日期、花型、开始/结束、缝纫时长、平均缝纫时长。
- 报警统计列表字段已按需求补齐：设备、员工工号/姓名、报警时间、报警信息。

3. 导出
- 已同步更新加工概况/时长统计/报警统计导出列，避免“页面与导出字段不一致”。

4. 验证
- 后端 `go test ./...` 通过。
- 前端 `npm run build` 通过（存在既有 Sass deprecation 与 chunk 警告，不影响构建成功）。

## 备注为后续修改（TODO）

1. 时间与速度精度
- 当前开始/结束时间、缝纫速度由现有字段推导（`created_at`、`running_time`、`stitches`）。
- 后续建议：在生产记录中新增真实 `start_time`、`end_time`、`sew_speed` 字段，改为真实值统计。

2. 报警与员工精确关联
- 当前报警记录员工信息按“设备+日期”映射生产记录得到，属于近似关联。
- 后续建议：报警记录直接落 `employee_id` 或关联上下文，保证报警归属精准。

3. 花型工价精确口径
- 当前工资下钻单价按“员工+设备+日期”工资记录映射。
- 后续建议：引入花型工价明细（或生产记录直接记录单价），实现按花型精确核算。

4. 前端可读性优化
- 状态统计三张表字段较多，建议后续增加“列显隐设置/冻结关键列/横向滚动优化”。

## 相关文件

- 后端主逻辑：`boer-lan-server/internal/api/statistics.go`
- 前端页面：
  - `boer-lan-client/src/views/statistics/SalaryStats.vue`
  - `boer-lan-client/src/views/statistics/ProcessOverview.vue`
  - `boer-lan-client/src/views/statistics/DurationStats.vue`
  - `boer-lan-client/src/views/statistics/AlarmStats.vue`

